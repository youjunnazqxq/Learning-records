---
Note: 
---
<%*
// ---------- 0) 强制英文 locale（锁定） ----------
window.moment.locale("en");               // 全局 moment 语言
let m = window.moment;                    // 缩写，后面用 m(...)
// ---------- 1. 从文件名解析 年 / 月 ----------
let title = tp.file.title;

// 支持：2026-M01 / 2026-M1 / 2026-01
let m1 = title.match(/^(\d{4})-M(\d{1,2})$/);
let m2 = title.match(/^(\d{4})-(\d{2})$/);

if (!m1 && !m2) {
  new Notice("⚠️ 文件名不符合 YYYY-M01 格式");
}

let year  = m1 ? m1[1] : m2 ? m2[1] : tp.date.now("YYYY");
let month = m1 ? parseInt(m1[2], 10) : m2 ? parseInt(m2[2], 10) : 1;

// ---------- 2. 构造当月第一天 ----------
let firstDay = window.moment(
  `${year}-${String(month).padStart(2, "0")}-01`,
  "YYYY-MM-DD"
);

// ---------- 3. 构造当月最后一天 ----------
let lastDay = firstDay.clone().endOf("month");
// ---------- 3. ✅ 识别季度 ----------
let toquarter_num = Math.floor((month - 1) / 3) + 1;          // 1~4
let toquarter = `Q${String(toquarter_num).padStart(2, "0")}`; // Q01~Q04
let quarterTitle = `${year}-${toquarter}`;                    // 2026-Q01
// ---------- 4. 你原来用到的变量（统一替换） ----------
let today   = firstDay.format("YYYY-MM-DD");   // ✅ 当月第一天
let toweek  = firstDay.format("[W]ww");         // ✅ 当月第一周
let tomonth = firstDay.format("MMMM");          // January
let toyear  = firstDay.format("YYYY");

// 获取当前日期
let today0 = tp.date.now("YYYY-MM-DD");
// 获取输入到inputDate
let inputDate = today;

// 创建一个 Date 对象，表示当前日期
let currentDate = new Date (inputDate);
firstDayFormatted = window.moment(firstDay, "MM-DD_ddd", true).add(0,"days").format("MM-DD_ddd");
lastDayFormatted = window.moment(lastDay, "MM-DD_ddd", true).add(0,"days").format("MM-DD_ddd");

toweek = window.moment(firstDay, "MM-DD_ddd", true).add(0,"days").format("[W]ww");
tomonth = window.moment(firstDay, "MM-DD_ddd", true).add(0,"days").format("MMMM");
Datecom1 = window.moment(firstDay, "MM-DD_ddd", true).add(0,"days").format("yyyy-MM-DD");
Datecom2 = window.moment(lastDay, "MM-DD_ddd", true).add(0,"days").format("yyyy-MM-DD");
toyear = window.moment(firstDay, "MM-DD_ddd", true).add(0,"days").format("YYYY");

let arr = []
let arr0 = []
arr[0]=window.moment(inputDate, "YYYY-MM-DD", true).format("YYYY-[W]ww")
arr0[0]=inputDate
for (let i = 1; i < 4; i++) {
	arr[i] = window.moment(arr0[i-1], "YYYY-MM-DD", true).add(7,"days").format("YYYY-[W]ww")
	arr0[i]= window.moment(arr0[i-1], "YYYY-MM-DD", true).add(7,"days")
}

-%>
<%*
// 运行后光标到运行后文件中
tp.file.cursor()
-%>
#Note/life/<%toyear%>/month

| [[<% arr[0] %>]] | [[<% arr[1] %>]] | [[<% arr[2] %>]] | [[<% arr[3] %>]] |
| ---------------- | ---------------- | ---------------- | ---------------- |
|                  |                  |                  |                  |

# 🌌 Abstract

```contributionGraph
title: 
graphType: month-track
showCellRuleIndicators: true
titleStyle:
  textAlign: center
  fontSize: 20px
  fontWeight: normal
dataSource:
  type: PAGE
  value: "#Note/life/daily"
  dateField:
    type: FILE_NAME
    format: yyyy-MM-DD
  countField:
    type: PAGE_PROPERTY
    value: Score
fillTheScreen: true
enableMainContainerShadow: false
cellStyleRules:
  - id: Ocean_a
    color: "#cae7ecff"
    min: "1"
    max: "20"
    text: 😅
  - id: Ocean_b
    color: "#adccecff"
    min: "20"
    max: "30"
  - id: Ocean_c
    color: "#8ea6c8ff"
    min: "30"
    max: "40"
  - id: Ocean_d
    color: "#5d81a1ff"
    min: "40"
    max: "50"
    text: ""
  - id: 1716786628139
    min: "50"
    max: "70"
    color: "#21537fff"
    text: ""
  - id: 1716786666392
    min: "70"
    max: "1000"
    color: "#04234bff"
    text: 🥳
mainContainerStyle:
  boxShadow: rgba(0, 0, 0, 0.16) 0px 1px 4px
cellStyle:
  minWidth: 17px
  minHeight: 17px
  borderRadius: ""
dateRangeType: FIXED_DATE_RANGE
fromDate: <%Datecom1%>
toDate: <%Datecom2%>
```

```dataview
table weeknote as 一句话描述一整周, Score
from "01. Daily/Weekly" and #Note/life/<%toyear%>/month/<% tomonth %>
sort file.name desc
```

>[!tip] 本季度核心目标
> ![[<% quarterTitle %>#🎯核心目标]]

# ✨ 最值得将时间投资在什么事情上？

- [ ] 

# 💭 每月回顾 

## 学术上

## 副业成就

## 缺点


# 🔍具体细节

## 👍 本月核心成就 

```add-summary
tags: #Note/life/yeah 
includeNote: #Note/life/<%toyear%>/month/<% tomonth %>
```

## 😵 本月核心困难

```add-summary
tags: #Note/life/hard 
includeNote: #Note/life/<%toyear%>/month/<% tomonth %>
```

## 💡 本月灵感库 

```add-summary
tags: #Note/life/Memos  
includeNote: #Note/life/<%toyear%>/month/<% tomonth %>
```

## 🥳 生活趣事 

```add-summary
tags: #Note/life/mood/Core 
includeNote: #Note/life/<%toyear%>/month/<% tomonth %>
```
