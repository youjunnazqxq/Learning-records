- **完整地址 (Full URL):** （你提供的原地址）
    
- **基础前缀 (baseURL):** 帮你提取出协议、域名和端口部分（用于全局配置）。
    
- **请求路径 (url):** 帮你提取出具体的路由部分（用于具体的 API 函数）。
    
- **查询参数 (params):** 如果地址里带有 `?` 后面的参数，我会帮你整理成可以在 Axios 中直接使用的 JavaScript 对象格式。