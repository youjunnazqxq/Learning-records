# className和classlist

在很久以前，JavaScript 中有一个限制：像 `"class"` 这样的保留字不能用作对象的属性。这一限制现在已经不存在了，但当时就不能存在像 `elem.class` 这样的 `"class"` 属性。
## className(查找属性)

```
<body class = 'main page'>
	<script>
		alert(document.body.className)//main page 
	</script>
</body>

```

## classList（修改属性）

`elem.classList` 是一个特殊的对象，它具有 `add/remove/toggle` 单个类的方法。

因此，我们既可以使用 `className` 对完整的类字符串进行操作，也可以使用使用 `classList` 对单个类进行操作。我们选择什么取决于我们的需求。 

- `[elem.classList.add/remove(class)](http://elem.classlist.add/remove\(class\))` —— 添加/移除类。
- `elem.classList.toggle(class)` —— 如果类不存在就添加类，存在就移除它。
- `elem.classList.contains(class)` —— 检查给定类，返回 `true/false`。


# 元素样式 

`elem.style` 属性是一个对象，它对应于 `"style"` 特性（attribute）中所写的内容。`elem.style.width="100px"` 的效果等价于我们在 `style` 特性中有一个 `width:100px` 字符串。

例如
```
document.body.style.cackgroundColor = prompt('background')
```


## 重置样式属性 

为了隐藏一个元素，我们可以设置 `elem.style.display = "none"`
我们可能想要移除 `style.display`，就像它没有被设置一样。这里不应该使用 `delete elem.style.display`，而应该使用 `elem.style.display = ""` 将其赋值为空。

`style.display` 设置为空字符串，那么浏览器通常会应用 CSS 类以及内建样式，就好像根本没有这样的 `style.display` 属性一样

 特殊的方法 `elem.style.removeProperty('style property')`。所以，我们可以像这样删除一个属性：

## 使用style.cssText进行完全重写 

`style.*` 来对各个样式属性进行赋值。我们不能像这样的 `div.style="color: red; width: 100px"` 设置完整的属性，因为 `div.style` 是一个对象，并且它是只读的。

```
<div id = 'div'>Button</div>
<script>
	div.style.cssText = 'color:red!important'
	background-color:yellow;
</script>
```
我们很少使用这个属性，因为这样的赋值会删除所有现有样式：它不是进行添加，而是替换它们。有时可能会删除所需的内容。但是，当我们知道我们不会删除现有样式时，可以安全地将其用于新元素。


# 注意单位 

不要忘记将 CSS 单位添加到值上。



# 计算样式：getcomputedstyle

**`style` 属性仅对 `"style"` 特性（attribute）值起作用，而没有任何 CSS 级联（cascade）。**

## getcomputedstyle
```
getComputedStyle(element,[pseudo])
```

element

需要被读取样式值的元素。

pseudo

伪元素（如果需要），例如 `::before`。空字符串或无参数则意味着元素本身。

