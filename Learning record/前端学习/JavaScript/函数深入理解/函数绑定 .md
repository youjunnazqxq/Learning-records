# 丢失this的情况


```
let user = { firstName: "John", 
sayHi() {
 alert(`Hello, ${this.firstName}!`); } };
 setTimeout(user.sayHi, 1000); // Hello, undefined!
```
对于这里的this指针来说他会返回undefined，因为在此，setTimeout获得的是一个函数的名字，这里函数

```
let f = user.sayHi;
setTimeout(f,1000);
```
这里user丢失了上下文，
