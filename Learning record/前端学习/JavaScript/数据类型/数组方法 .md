# splice

## 用delete删除对象

```
let arr = ["I","go","home"];

delete arr[1]；

alert( arr.length );//3
```

元素被删除了，但数组仍有三个元素，

## splice方法 

```javascript
arr.splice(start, deleteCount, elem1, ..., elemN)
```

它从索引 `start` 开始修改 `arr`：删除 `deleteCount` 个元素并在当前位置插入 `elem1, ..., elemN`。在原数组修改之后返回被删除元素组成的新数组。


```javascript
let arr = ["I", "study", "JavaScript"];

arr.splice(1, 1); // 从索引 1 开始删除 1 个元素

alert( arr ); // ["I", "JavaScript"]
```

简单，对吧？从索引 `1` 开始删除 `1` 个元素。（译注：当只填写了 `splice` 的 `start` 参数时，将删除从索引 `start` 开始的所有数组项）

在下一个例子中，我们删除了 3 个元素，并用另外两个元素替换它们：
```javascript
let arr = ["I", "study", "JavaScript", "right", "now"];

// 删除数组的前三项，并使用其他内容代替它们
arr.splice(0, 3, "Let's", "dance");

alert( arr ) // 现在 ["Let's", "dance", "right", "now"]
```

在这里我们可以看到 `splice` 返回了被删除的元素所组成的数组：

```javascript
let arr = ["I", "study", "JavaScript", "right", "now"];

// 删除前两个元素
let removed = arr.splice(0, 2);

alert( removed ); // "I", "study" <-- 被从数组中删除了的元素
```

我们可以将 `deleteCount` 设置为 `0`，`splice` 方法就能够插入元素而不用删除任何元素：


```javascript
let arr = ["I", "study", "JavaScript"];

// 从索引 2 开始
// 删除 0 个元素
// 然后插入 "complex" 和 "language"
arr.splice(2, 0, "complex", "language");

alert( arr ); // "I", "study", "complex", "language", "JavaScript"
```

## slice

slice方法
```
arr.slice([strat],[end]);
```
它会返回一个新数组，将所有从索引 `start` 到 `end`（不包括 `end`）的数组项复制到一个新的数组。`start` 和 `end` 都可以是负数，在这种情况下，从末尾计算索引。


## concat 

```javascript
arr.concat(arg1, arg2...)
```

它接受任意数量的参数 —— 数组或值都可以。

结果是一个包含来自于 `arr`，然后是 `arg1`，`arg2` 的元素的新数组。

如果参数 `argN` 是一个数组，那么其中的所有元素都会被复制。否则，将复制参数本身。

例如：

```javascript
let arr = [1, 2];

// 从 arr 和 [3,4] 创建一个新数组
alert( arr.concat([3, 4]) ); // 1,2,3,4

// 从 arr、[3,4] 和 [5,6] 创建一个新数组
alert( arr.concat([3, 4], [5, 6]) ); // 1,2,3,4,5,6

// 从 arr、[3,4]、5 和 6 创建一个新数组
alert( arr.concat([3, 4], 5, 6) ); // 1,2,3,4,5,6
```

通常，它只复制数组中的元素。其他对象，即使它们看起来像数组一样，但仍然会被作为一个整体添加：

```javascript
let arr = [1, 2];

let arrayLike = {
  0: "something",
  length: 1
};

alert( arr.concat(arrayLike) ); // 1,2,[object Object]
```

……但是，如果类数组对象具有 `Symbol.isConcatSpreadable` 属性，那么它就会被 `concat` 当作一个数组来处理：此对象中的元素将被添加：
```javascript
let arr = [1, 2];

let arrayLike = {
  0: "something",
  1: "else",
  [Symbol.isConcatSpreadable]: true,
  length: 2
};

alert( arr.concat(arrayLike) ); // 1,2,something,else
```


# forEach

arr.forEach 方法运行为为每一个元素都运行一个方法

基本语法：


```
arr,forEach(function(item,index,array)){
}
```

```
let arr = [1,2,3];

arr.forEach(function(item,index,array)=>{
	alert('${item} is at index ${index} in ${array}')
})
```

对于这个方法来说，这个传入的函数必须按照`item index array`的顺序传入参数，可以不全部传入，但必须要按照顺序。


# 数组中的搜索方法

## includes

```
arr.includes(item,from)//item需要找的值，from：从索引开始的位置
```

这个方法当找到目标值返回true，没有返回false。

## indexof

与includes的用法基本一样，但是返回值有所不同，找到返回该值，没有找到返回-1.

>[!note ]
>includes可以正确找到NaN，并返回true
>indexOf不行，返回-1.
>
>

## find


```
let result = arr,find(function(item,index,array){
	//函数如果返回true，则返回item并停止；
	//函数如果返回false，则返回undefined

})
```


## filter
与find类型，但是这个方法返回的是所有匹配元素所组成的数组；


# 转化数组

## map

```
	let result = arr.map(function(item,index,array）｛  //元素的修改过程 
	//返回对单一元素的修改后的值

})
```

## sort

```
 arr.sort(compareNumber);

function compareNumber(a,b){
	return a-b;
}
//也可以使用箭头函数
arr.sort((a,b)=>a-b);
```

对于sort方法来说，他需要接受一个如何判断大小的函数来支持他来排序。


## reverse
对原数字进行颠倒

## split

```
let arr= names.split(',');
```
给定一个字符串，他以传入的参数为分隔符，创造一个数组

## join

与split相反的过程

## reduce

```
let value = arr.reduce(function(accumulator,item,index,array),[inital])
```

- accumulator-上一次函数调用的结果，第一次等于initial

## Array.isArray

判断是否为一个数组