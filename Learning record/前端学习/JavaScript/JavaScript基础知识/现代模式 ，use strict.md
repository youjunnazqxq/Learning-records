# use strict（严格模式）

当这段代码处于文件顶部的时候，就会开启现代模式

确保其属于最顶部

一旦开启无法退回 

局部启用的灵活用法：
它不仅能在文件顶部全局启用，也可以局部作用于单个函数（把 "use strict" 写在函数内部的最开头）——这样只有该函数会进入严格模式，不影响其他代码。

​
- 全局作用域中 this 是 undefined （非严格模式下是 window ，所以别在全局用 this 代替全局对象）；





# 作用

1.禁用老版本中被废弃的代码

2.规范代码 



