# 术语： 运算元，运算符

- **运算元** —— 运算符应用的对象。比如说乘法运算 `5 * 2`，有两个运算元：左运算元 `5` 和右运算元 `2`。有时候人们也称其为“参数”而不是“运算元”。
    
- 如果一个运算符对应的只有一个运算元，那么它是 **一元运算符**。比如说一元负号运算符（unary negation）`-`，它的作用是对数字进行正负转换：

 ```javascript
     let x = 1;
       x = -x;
    alert( x ); // -1，一元负号运算符生效
    ```

- 如果一个运算符拥有两个运算元，那么它是 **二元运算符**。减号还存在二元运算符形式：
   
   ```javascript
    let x = 1, y = 3;
    alert( y - x ); // 2，二元运算符减号做减运算
    ```

# 二元运算符 “+”

在通常中二元运算符+用于求和，在字符串中有如下应用
```
let s = "hello"+"word"
//注意 
alert("1"+2)//"12"
alert(2+2+"1")//"41"
```
加号运算符是唯一支持这种运算的，如果是-号
就会将字符串转化为数字进行运算



# 一元运算符 +

最主要的作用就是将非数字类转换为数字类，和Number（）差不多

