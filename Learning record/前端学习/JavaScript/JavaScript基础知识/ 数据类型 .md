# Number

number类代表整数和浮点数 


## infinity

Infinity （无穷大）在代码中通常用1/0来表示


## NaN
代表一个计算错误，与其有关的任何计算结果都会为NaN



# BigInt


当Number的数据大小超过2 * * 53 -1的时候，精度就会下降 


## 使用格式
```
const bigInt=11111111111111111111111n
```


# String 
三种表示方法
```
let str ="Hello"
let str2 ='word'
let str3 =`can embed another ${str}`;

```
对于双引号和单引号来说，就是普通的字符串，而对于反引号来说，${name},可以对这里面的值进行解析，
变量-变量的值
表达式-求值
字符串-字符串


# Boolean 

布尔值

# null

特殊的 `null` 值不属于上述任何一种类型。

它构成了一个独立的类型，只包含 `null` 值：

```javascript
let age = null;
```

相比较于其他编程语言，JavaScript 中的 `null` 不是一个“对不存在的 `object` 的引用”或者 “null 指针”。

JavaScript 中的 `null` 仅仅是一个代表“无”、“空”或“值未知”的特殊值。

上面的代码表示 `age` 是未知的。


# undefined

特殊值 `undefined` 和 `null` 一样自成类型。

`undefined` 的含义是 `未被赋值`。

如果一个变量已被声明，但未被赋值，那么它的值就是 `undefined`：
他的含义就是 未被赋值

```javascript
let age;

alert(age); // 弹出 "undefined"
```


# typeof

typeof + 数据 ，会返回参数的数据类型 

>[!note]
>注意：typeof null 会返回object，实际上null并不是object，这是语言的一个错误
>注意 typeof 方法会返回一个“字符串” ，例如“object” “Number”
>typeof function(){}//会返回“function”


