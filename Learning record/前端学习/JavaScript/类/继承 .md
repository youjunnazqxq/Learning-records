# extend
```
class Rabbit extend Animal
```

![[Pasted image 20251021200927.png]]

## 注意 
在extends后面可以允许任意的表达式，它会进行运算后的结果


# 重写方法 

## super关键字

super.method()调用父类方法
super()调用父类的构造器（只能在外面的constructor中使用）

>[!note]
>箭头函数没有super关键字，它会和this一样从外部函数中获取



# 重写constructor


**继承类的 constructor 必须调用 `super(...)`，并且 (!) 一定要在使用 `this` 之前调用。**

在 JavaScript 中，继承类（所谓的“派生构造器”，英文为 “derived constructor”）的构造函数与其他函数之间是有区别的。派生构造器具有特殊的内部属性 `[[ConstructorKind]]:"derived"`。这是一个特殊的内部标签。

该标签会影响它的 `new` 行为：

- 当通过 `new` 执行一个常规函数时，它将创建一个空对象，并将这个空对象赋值给 `this`。
- 但是当继承的 constructor 执行时，它不会执行此操作。它期望父类的 constructor 来完成这项工作。

因此，派生的 constructor 必须调用 `super` 才能执行其父类（base）的 constructor，否则 `this` 指向的那个对象将不会被创建。并且我们会收到一个报错。

## 字段有关问题 
实际上，原因在于字段初始化的顺序。类字段是这样初始化的：

- 对于基类（还未继承任何东西的那种），在构造函数调用前初始化。
- 对于派生类，在 `super()` 后立刻初始化。

所以当我的子代没有constructor的时候，调用父代的时候，此时只有父代有这个字段，而子代没有

# `[[HomeObject]]`

当一个函数被定义为类或者对象方法时，它的 `[[HomeObject]]` 属性就成为了该对象。

然后 `super` 使用它来解析（resolve）父原型及其方法。

`[[HomeObject]]` 是一个在方法定义时就已固定的“静态指针”，它指向该方法被定义时所在的那个对象（即原型），其唯一目的是作为 `super` 关键字向上查找的“起点”