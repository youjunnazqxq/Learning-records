内建的方法例如 `filter`，`map` 等 —— 返回的正是子类 `PowerArray` 的新对象。它们内部使用了对象的 `constructor` 属性来实现这一功能。

当arr.filter()被调用时，它的内部使用的是arr.constructor来创建数组，而不是使用原生的Array。
当然我们可以改变其，有一整特殊的静态方法`Symbol.species`
这个方法会返回创建新实体的`constructor`

# 内建类没有静态方法继承

内建的类之间，静态的方法不会继承![[Capture_20251022_191224.jpg]]