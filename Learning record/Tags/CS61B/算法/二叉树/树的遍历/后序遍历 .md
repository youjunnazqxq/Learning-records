```
function postorder_traversal(node): // 如果节点为空，就返回 if node is null: return // 1. 首先，递归地遍历左子树 postorder_traversal(node.left) // 2. 接着，递归地遍历右子树 postorder_traversal(node.right) // 3. 最后，处理（比如打印）当前根节点的值 print(node.value)
```