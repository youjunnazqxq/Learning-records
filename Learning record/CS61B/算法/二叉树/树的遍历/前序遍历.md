代码结构
```
function preorder_traversal(node): // 如果节点为空，就返回 

if node is null: return // 1. 首先处理（比如打印）当前根节点的值
 
 print(node.value) // 2. 然后，递归地遍历左子树 
 
 preorder_traversal(node.left) // 3. 最后，递归地遍历右子树
 
 preorder_traversal(node.right)
```




