- **有序/基本有序时谁最快？** -> **直接插入排序**。
    
- **有序/基本有序时谁最慢？** -> **快速排序**。
    
- **谁是不稳定的？** -> **“快些（希）选一堆”**（快速、希尔、选择、堆）。
    
- **谁一趟排序不能确定一个最终位置？** -> **归并排序**（还有希尔排序）。
    
- **想找前 K 个最大/最小？** -> **堆排序**。
    
- **最坏情况下（所有情况）都很好的算法？** -> **堆排序** 和 **归并排序**（都是 $O(n \log n)$，但堆排序更省空间）。