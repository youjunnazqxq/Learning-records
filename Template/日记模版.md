<%*
// ---------- 0) 强制英文 locale（锁定） ----------
window.moment.locale("en");

// 按照年-月-日的格式，得到今天日期的变量（例如2023-03-07）
let today = tp.date.now("MM-DD")
let today_full = tp.date.now("YYYY-MM-DD")
let title = tp.file.title
//let inputDate = await tp.system.prompt("输入示例："+today_full , today_full)
inputDate = title
// 将输入的日期字符串转换为日期对象
let parsedDate = new Date(inputDate)

// 获取周数和月份

// 提取月份名称（如 "October"）
const monthNames = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"];
let tomonth = monthNames[parsedDate.getMonth()];

// 计算ISO周数函数
function getISOWeek(date) {
    const d = new Date(date);
    d.setHours(0, 0, 0, 0);
    d.setDate(d.getDate() + 4 - (d.getDay() || 7));
    const yearStart = new Date(d.getFullYear(), 0, 1);
    return Math.ceil(((d - yearStart) / 86400000 + 1) / 7);
}

let toweek = `W${getISOWeek(parsedDate).toString().padStart(2, '0')}`;
let toyear = String(parsedDate.getFullYear())
// 获取当前文件创建时间
let createTime = tp.file.creation_date()
// 获取当前文件修改时间
let modificationDate = tp.file.last_modified_date("dddd Do MMMM YYYY HH:mm:ss")

-%>
<%*
// 运行后光标到运行后文件中
tp.file.cursor()
-%>
---
daynote: 
Score: 0 
Study Score: 0
Entertainment Score: 0
Habit Score: 0
(🍭3) 早起: false
(🍭2) 午睡: false
(🍭10) 运动: false
(🍭5) 早睡: false
(🍭2) 单词: false
(🍭5) 论文: false
(🍭3) GPT口语: false

---
#Note/life/daily #Note/life/<% toyear %>/weekly/<% toweek %> #Note/life/<% toyear %>/month/<% tomonth %>
# ➤ Direction

>[!tip] Weekly Plans
> ![[01. Daily/Weekly/<% toyear %>-<% toweek %>#📌 本周最高优先级]]

# 😉 Get already?

## 📍Task

- [ ] 

# 🤔 Review

## 🌾 Gains

### ⚡️Academic gains

### 🌸 Interesting gains


## 🥺Shortcoming


## 🌌 Realisation


## 🪄Memos


## ❤️ The art of inside and out

